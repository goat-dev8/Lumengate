{
  "name": "lumengate-scripts",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "npm --prefix app run test && npm --prefix issuer-service run test"
  },
  "dependencies": {
    "ethers": "^6.13.0",
    "express": "^4.21.0"
  }
}
